import styles from '../style'

const Stats = () => (
    <div className="stats p-2 w-full flex items-center justify-between">
        <div className="user__active text-[40px] flex items-center text-white">
          3800+ <span className={`text-gradient text-[20px]`}>USER ACTIVE</span>
        </div>
    </div>
)
export default Stats