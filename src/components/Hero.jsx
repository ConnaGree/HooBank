import styles from "../style";
import { discount, robot } from "../assets";

const Hero = () => (
  <section
    id="home"
    className={`flex md:flex-row justify-between pl-20 flex-col ${styles.paddingY}`}
  ></section>
);
export default Hero;
